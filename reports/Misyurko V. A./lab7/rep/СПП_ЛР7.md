Министерство образования Республики Беларусь
Учреждение образования
«Брестский государственный технический университет»
Кафедра ИИТ

# Лабораторная работа №7
По дисциплине «Современные платформы программирования»

Выполнил:
Студент 3 курса
Группы ПО-12
Мисюрко В. А.
Проверил:
Кулик А. Д.

Брест 2026
## Цель работы: освоить возможности языка программирования Python в разработке
оконных приложений.

## Задание 1. Построение графических примитивов и надписей
Требования к выполнению
Требования к выполнению
• Реализовать соответствующие классы, указанные в задании;
• Организовать ввод параметров для создания объектов (использовать
экранные компоненты);
• Осуществить визуализацию графических примитивов
14) Создать классы Point и Line. Объявить список из n объектов класса Point и
определить в методе, какая из точек находится дальше всех от прямой линии.

## Решение:

### 1.1. Классы Point и Line

```python
@dataclass(slots=True)
class Point:
x: float
y: float

def distance_to_line(self, line: "Line") -> float:
numerator = abs(line.a * self.x + line.b * self.y + line.c)
denominator = math.sqrt(line.a**2 + line.b**2)
return numerator / denominator

@dataclass(slots=True)
class Line:
a: float
b: float
c: float
```

### 1.2. Генерация точек и подготовка данных

```python
def generate_data(self) -> None:
point_range = float(self.range_var.get())
point_count = int(self.count_var.get())

line = Line(
float(self.a_var.get()),
float(self.b_var.get()),
float(self.c_var.get()),
)

random.seed(int(self.seed_var.get()))
self.points = [
Point(
x=random.uniform(-point_range, point_range),
y=random.uniform(-point_range, point_range),
)
for _ in range(point_count)
]
self.line = line
```

### 1.3. Поиск самой удалённой точки

```python
def _animate_step(self) -> None:
if self.current_index >= len(self.points):
return

current_point = self.points[self.current_index]
current_distance = current_point.distance_to_line(self.line)
if current_distance > self.best_distance:
self.best_distance = current_distance
self.best_index = self.current_index

self.current_index += 1
self.animation_job = self.root.after(
int(self.speed_var.get()), self._animate_step
)
```

### 1.4. Построение сцены

```python
def _draw_scene(self) -> None:
self.axis.clear()
self.axis.set_title("Точки и прямая")
self.axis.set_xlabel("X")
self.axis.set_ylabel("Y")

if self.points:
x_values = [point.x for point in self.points]
y_values = [point.y for point in self.points]
self.axis.scatter(x_values, y_values, color="#2b6cb0", label="Точки")

vertical_x = self.line.x_constant()
if vertical_x is not None:
self.axis.axvline(vertical_x, color="#1a202c", linewidth=2,
label="Прямая")
```

### 1.5. Пауза и сохранение скриншота

```python
def toggle_pause(self) -> None:
if not self.points:
return
self.is_paused = not self.is_paused

def save_screenshot(self) -> None:
timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
output_path = os.path.join(os.getcwd(), f"geometry_scene_{timestamp}.png")
self.figure.savefig(output_path, dpi=180)
```

## Задание 2. Реализовать построение заданного типа фрактала по варианту
14) Бассейны Ньютона

## Задание 2. Фрактал «Бассейны Ньютона»

### 2.1. Подготовка сетки для фрактала

```python
def _prepare_grid(self) -> None:
degree = self.degree_var.get()
resolution = self.resolution_var.get()
plane_range = self.range_var.get()

axis_values = np.linspace(-plane_range, plane_range, resolution)
self.grid_real, self.grid_imag = np.meshgrid(axis_values, axis_values)
self.result = np.zeros((resolution, resolution, 3), dtype=float)
self.roots = np.array(
[np.exp(2j * np.pi * root / degree) for root in range(degree)],
dtype=complex,
)
```

### 2.2. Запуск построения

```python
def start_render(self) -> None:
self._cancel_render()
self._prepare_grid()
self.status_var.set("Построение началось.")
self._render_next_row()
```

### 2.3. Вычисление строки фрактала

```python
def _compute_row(self, row_complex: np.ndarray) -> np.ndarray:
degree = self.degree_var.get()
max_iterations = self.iterations_var.get()
tolerance = self.tolerance_var.get()

z_values = row_complex.copy()
iteration_counts = np.zeros_like(z_values, dtype=int)

for iteration in range(max_iterations):
derivative = degree * np.power(z_values, degree - 1)
safe_mask = np.abs(derivative) > 1e-12
z_values[safe_mask] = z_values[safe_mask] - (
(np.power(z_values[safe_mask], degree) - 1) / derivative[safe_mask]
)
```

### 2.4. Пошаговая визуализация

```python
def _render_next_row(self) -> None:
if self.current_row >= self.total_rows:
self.status_var.set("Построение завершено.")
return

row_complex = self.grid_real[self.current_row] + 1j *
self.grid_imag[self.current_row]
rendered_row = self._compute_row(row_complex)
self.result[self.current_row] = rendered_row
self.current_row += 1

self._draw_current_result()
self.render_job = self.root.after(int(self.speed_var.get()),
self._render_next_row)
```

### 2.5. Отображение результата

```python
def _draw_current_result(self) -> None:
plane_range = self.range_var.get()
self.axis.clear()
self.axis.set_title(
f"Бассейны Ньютона для z^{self.degree_var.get()} - 1"
)
self.axis.imshow(
self.result,
origin="lower",
extent=(-plane_range, plane_range, -plane_range, plane_range),
)
self.canvas.draw_idle()
```

### 2.6. Пауза и сохранение изображения

```python
def toggle_pause(self) -> None:
if self.result is None:
return
self.is_paused = not self.is_paused

def save_screenshot(self) -> None:
timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
output_path = os.path.join(os.getcwd(), f"newton_basins_{timestamp}.png")
self.figure.savefig(output_path, dpi=180)
```

Вывод: освоил возможности языка программирования Python в разработке оконных
приложений.
