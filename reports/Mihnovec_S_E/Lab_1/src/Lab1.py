"""Модуль для работы со списками: создание, сравнение, добавление и удаление элементов."""


def createlist() -> list[int]:
    """
    Позволяет создать список и заполнить его целыми числами.

    :return: список целых чисел
    """
    objects = []
    try:
        n = int(input("Число объектов в списке: "))
        for i in range(n):
            element = int(input(f"Введите элемент {i+1}: "))
            objects.append(element)
            print(f"Элемент {element} был добавлен в список.")
            print(f"Итоговый список: {objects}")
    except ValueError:
        print("Ошибка: нужно вводить только целые числа!")
    return objects


def isequal(objects: list[int]) -> str:
    """
    Проверяет, равны ли элементы списка.

    :param objects: Список целочисленных элементов
    :return: Строка с результатом сравнения
    """
    if len(objects) < 2:
        print("Нужно минимум 2 элемента для сравнения!")
        return "меньше двух."

    if len(set(objects)) == 1:
        print("Все элементы в списке равны.")
        return "равны"

    print("В списке есть разные элементы.")
    return "неравны"


def add_item(objects: list[int]):
    """Добавляет элемент в список."""
    print("Вы выбрали: добавление")
    try:
        element = int(input("Введите элемент: "))
        objects.append(element)
        print(f"Элемент {element} был добавлен в список.")
    except ValueError:
        print("Ошибка: нужно вводить только целые числа!")


def remove_item(objects: list[int]):
    """Удаляет элемент из списка по индексу."""
    print("Вы выбрали: удаление")
    try:
        idx = int(input("Введите номер элемента: ")) - 1
        try:
            removed = objects.pop(idx)
            print(f"Элемент {removed} под номером {idx + 1} был удален.")
        except IndexError:
            print("Индекс вышел за пределы списка!")
    except ValueError:
        print("Ошибка: нужно вводить только целые числа!")


def main():
    """Основная функция управления программой."""
    objects: list[int] = []

    while True:
        print("\n--- Хелло👋! ---")
        print("1. Показать список\n2. Создать список\n3. Добавить элемент")
        print("4. Удалить элемент\n5. Сравнить элементы\n6. Выход")

        choice = input("\nВведите выбор меню: ")

        if choice == "1":
            print(f"Список: {objects}")
        elif choice == "2":
            objects = createlist()
        elif choice == "3":
            add_item(objects)
        elif choice == "4":
            remove_item(objects)
        elif choice == "5":
            result = isequal(objects)
            print(f"Элементы(ов) {result}.")
        elif choice == "6":
            print("Гу ба-и👋!")
            break
        else:
            print("Ошибка! Введите число от 1 до 6.")


if __name__ == "__main__":
    main()
